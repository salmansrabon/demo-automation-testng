# demo-automation-testng
How to run this project:
- Clone the repo
- go to project root directory and give these command one after one: 
``` 
gradle clean test 
allure generate allure-results --clean -o allure-report
allure serve allure-results
```
